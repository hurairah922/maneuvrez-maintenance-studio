# Active Feature Spec

There is no active implementation spec at the moment.

The most recently completed spec is:

- `specs/features/done/001-plugin-shell.md`

Recommended next spec:

- `002-admin-settings-foundation.md`

Before starting the next phase:

- verify the Phase 1 plugin shell inside a real WordPress install
- record any runtime issues found during activation, settings save, or frontend routing
- create the next active spec with concrete acceptance criteria
