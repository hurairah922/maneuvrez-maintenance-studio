# Codex Task: Update Active Spec

This prompt has been merged into:

    specs/prompts/codex/000-normalize-project-docs.md

Use that prompt as the single source of truth for:

- project identity normalization
- Phase 1 scope normalization
- folder structure normalization
- active spec updates

Keep this file only as a compatibility pointer so older references do not break.
